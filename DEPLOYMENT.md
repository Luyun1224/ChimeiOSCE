# 部署指南

## 📦 已完成的模組化工作

### ✅ 完成項目

1. **程式碼分離** - 已將超過 800 行的內嵌 JavaScript 分離成 8 個模組
2. **配置管理** - API URLs 集中在 `config.js`
3. **打包壓縮** - 使用 Webpack + Terser 壓縮混淆
4. **檔案大小** - 原始程式碼約 50KB，壓縮後僅 28KB

### 📁 專案結構

```
js/
├── src/              ← 原始碼 (開發維護用)
│   ├── config.js     (API 配置)
│   ├── state.js      (狀態管理)
│   ├── sp-analysis.js
│   ├── examiner-feedback.js
│   ├── student-feedback.js
│   ├── sp-feedback.js
│   ├── ui.js
│   └── main.js
└── app.min.js        ← 打包後檔案 (生產環境)
```

## 🚀 使用方式

### 開發環境

當您需要修改程式碼時:

```bash
# 1. 安裝依賴 (首次)
npm install

# 2. 修改 js/src/ 中的任何檔案

# 3. 打包
npm run build

# 4. 測試
npm run serve
# 開啟 http://localhost:8000
```

### 生產環境部署

只需上傳以下檔案:
- ✅ `index.html`
- ✅ `js/app.min.js`
- ❌ 不需要 `js/src/` 目錄
- ❌ 不需要 `node_modules/`

## 🔐 程式碼保護效果

### 原始碼 (js/src/config.js)
```javascript
export const API_CONFIG = {
    SP_API_URL: "https://script.google.com/macros/...",
    FEEDBACK_API_URL: "https://script.google.com/macros/...",
    // ...
};
```

### 壓縮後 (app.min.js)
```javascript
(()=>{"use strict";const t={currentView:"dashboard"...
const n="https://script.google.com/macros/s/AKf...
```

✅ 變數名稱混淆 (currentView → t)
✅ 程式碼壓縮 (移除空白和換行)
✅ 移除註解
✅ 難以閱讀和逆向工程

## 🔧 常見維護任務

### 修改 API URL

編輯 `js/src/config.js`:
```javascript
export const API_CONFIG = {
    SP_API_URL: "新的 URL",
    // ...
};
```

然後執行:
```bash
npm run build
```

### 新增功能

1. 在對應的模組檔案中新增函數
2. 在 `main.js` 中整合
3. 執行 `npm run build`

### 除錯

使用開發模式:
```bash
npm run dev
```

這會監看檔案變更並自動重新編譯。

## 📊 效能提升

- **載入速度**: 壓縮後檔案更小，載入更快
- **可維護性**: 模組化結構，易於維護
- **程式碼安全**: 混淆處理，降低被複製風險

## 🛡️ 進一步保護 (可選)

如需更強保護，可考慮:

1. **環境變數**: 將 API URLs 移到環境變數
2. **API 金鑰**: 使用後端代理保護 API
3. **混淆工具**: 使用 javascript-obfuscator 加強混淆

```bash
npm install javascript-obfuscator --save-dev
```

修改 `package.json`:
```json
"scripts": {
  "build:obfuscate": "webpack --mode production && javascript-obfuscator js/app.min.js --output js/app.min.js --compact true"
}
```

## ❓ 常見問題

### Q: 為什麼瀏覽器顯示錯誤?
A: 確保已執行 `npm run build` 並且 `js/app.min.js` 存在。

### Q: 如何回復到舊版本?
A: 舊版 JavaScript 已從 index.html 移除，可從 Git 歷史恢復。

### Q: 程式碼還能被看到嗎?
A: 仍可通過開發者工具看到壓縮後的程式碼，但已難以閱讀和理解邏輯。完全隱藏需要後端 API 保護。

## 📞 技術支援

如有問題，請檢查:
1. Node.js 已安裝 (版本 14+)
2. 已執行 `npm install`
3. 已執行 `npm run build`
4. 瀏覽器控制台錯誤訊息

---

✨ **模組化完成! 現在您的程式碼更安全、更易維護了。**
