# 專案架構圖

## 🏗️ 整體架構

```
┌─────────────────────────────────────────────────────────┐
│                     index.html                          │
│                   (主要 HTML 檔案)                       │
└────────────────────┬────────────────────────────────────┘
                     │
                     │ 引用
                     ▼
           ┌─────────────────┐
           │  js/app.min.js  │  ◄── 打包壓縮後的程式碼
           │   (27.3 KB)     │
           └────────┬────────┘
                    │
                    │ 由 Webpack 打包自
                    ▼
    ┌───────────────────────────────────────┐
    │          js/src/ (原始模組)           │
    └───────────────────────────────────────┘
                    │
        ┌───────────┼───────────┐
        │           │           │
        ▼           ▼           ▼
    ┌────────┐  ┌────────┐  ┌────────┐
    │config.js│  │state.js│  │ ui.js  │
    │  (7行)  │  │ (25行) │  │(132行) │
    └────────┘  └────────┘  └────────┘
                    │
        ┌───────────┼───────────┐
        │           │           │
        ▼           ▼           ▼
┌──────────────┐┌──────────────┐┌──────────────┐
│sp-analysis.js││examiner-     ││student-      │
│   (274行)    ││feedback.js   ││feedback.js   │
│              ││   (215行)    ││   (264行)    │
└──────────────┘└──────────────┘└──────────────┘
        │           
        ▼           
┌──────────────┐┌──────────────┐
│sp-feedback.js││   main.js    │
│   (199行)    ││   (223行)    │
│              ││  (主入口)    │
└──────────────┘└──────────────┘
```

## 📊 資料流程

```
使用者操作
    │
    ▼
┌─────────────────┐
│    index.html   │
└────────┬────────┘
         │
         │ 載入
         ▼
┌─────────────────┐
│  app.min.js     │
│  ├─ main.js     │ ◄─── 初始化應用程式
│  ├─ ui.js       │ ◄─── 處理視圖切換
│  └─ state.js    │ ◄─── 管理全域狀態
└────────┬────────┘
         │
         │ 獲取數據
         ▼
┌─────────────────┐
│   config.js     │ ◄─── API URLs
└────────┬────────┘
         │
         │ API 請求
         ▼
┌──────────────────────────────┐
│    Google Apps Script APIs   │
│  ├─ SP 評估數據              │
│  ├─ 考官回饋數據             │
│  ├─ 學生回饋數據             │
│  └─ SP 回饋數據              │
└──────────┬───────────────────┘
           │
           │ 返回數據
           ▼
┌──────────────────────────────┐
│      分析模組處理數據         │
│  ├─ sp-analysis.js           │
│  ├─ examiner-feedback.js     │
│  ├─ student-feedback.js      │
│  └─ sp-feedback.js           │
└──────────┬───────────────────┘
           │
           │ 渲染結果
           ▼
┌──────────────────────────────┐
│      更新 UI (圖表、表格)     │
│  ├─ Chart.js 圖表            │
│  ├─ 統計數字                  │
│  ├─ 關鍵字分析                │
│  └─ 數據表格                  │
└──────────────────────────────┘
```

## 🔄 開發流程

```
┌─────────────────┐
│  修改原始模組    │
│   js/src/*.js   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  npm run build  │  ◄─── Webpack 打包
└────────┬────────┘
         │
         ├─ 編譯模組
         ├─ 打包整合
         ├─ Terser 壓縮
         └─ 混淆變數名
         │
         ▼
┌─────────────────┐
│  app.min.js     │  ◄─── 壓縮後檔案
│    (27.3 KB)    │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  npm run serve  │  ◄─── 本地測試
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   部署上線       │
│  (index.html +  │
│   app.min.js)   │
└─────────────────┘
```

## 🎯 模組功能分工

```
┌──────────────────────────────────────────────────┐
│                   main.js                        │
│              (應用程式主控制器)                    │
│  ├─ 初始化應用                                    │
│  ├─ 設置事件監聽器                                │
│  ├─ 協調各模組                                    │
│  └─ 處理視圖切換                                  │
└───────────────────┬──────────────────────────────┘
                    │
        ┌───────────┼───────────┐
        │           │           │
        ▼           ▼           ▼
┌──────────────┐┌──────────────┐┌──────────────┐
│   config.js  ││  state.js    ││   ui.js      │
│   API 配置   ││  狀態管理    ││  UI 控制     │
│              ││              ││              │
│ • SP_API     ││ • globalData ││ • switchView │
│ • FEEDBACK   ││ • charts     ││ • updateStat │
│ • STUDENT    ││ • state      ││ • toggleMenu │
│ • SP_FEEDBACK││              ││              │
└──────────────┘└──────────────┘└──────────────┘
        │
        ├───────────────┬───────────────┬──────────────┐
        │               │               │              │
        ▼               ▼               ▼              ▼
┌─────────────┐┌─────────────┐┌─────────────┐┌─────────────┐
│sp-analysis  ││examiner-    ││student-     ││sp-feedback  │
│   .js       ││feedback.js  ││feedback.js  ││   .js       │
│             ││             ││             ││             │
│ • fetchData ││ • fetchData ││ • fetchData ││ • fetchData │
│ • filterData││ • applyFilt ││ • applyFilt ││ • applyFilt │
│ • processData│• renderDash ││ • renderDash││ • renderDash│
│ • renderChart│• renderChart││ • renderChart│• renderChart│
│ • renderTable│• renderTable││ • renderTable│• renderTable│
└─────────────┘└─────────────┘└─────────────┘└─────────────┘
```

## 📦 打包過程

```
原始模組 (1,339 行)
        │
        ├─ config.js      (7 行)
        ├─ state.js       (25 行)
        ├─ ui.js          (132 行)
        ├─ main.js        (223 行)
        ├─ sp-analysis.js (274 行)
        ├─ examiner-feedback.js (215 行)
        ├─ student-feedback.js  (264 行)
        └─ sp-feedback.js (199 行)
        │
        ▼
    Webpack 打包
        │
        ├─ 解析模組依賴
        ├─ 合併所有模組
        ├─ 轉換 ES6 → ES5
        └─ 樹狀優化 (Tree Shaking)
        │
        ▼
    Terser 壓縮
        │
        ├─ 移除空白與換行
        ├─ 縮短變數名稱
        ├─ 移除註解
        └─ 優化程式碼
        │
        ▼
    app.min.js (27.3 KB)
        │
        └─ 單一壓縮檔案，難以閱讀
```

---

## 📈 效能對比

### 修改前
```
index.html
└─ <script> 800+ 行內嵌程式碼 </script>
   ├─ 完全可讀
   ├─ 難以維護
   └─ 約 50+ KB
```

### 修改後
```
index.html
└─ <script src="js/app.min.js"></script>
   ├─ 高度混淆
   ├─ 模組化易維護
   └─ 27.3 KB (減少 44%)
```
